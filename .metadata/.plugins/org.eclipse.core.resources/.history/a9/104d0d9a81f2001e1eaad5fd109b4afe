import java.util.Objects;

//	⦁	Implement an own LinkedList class as CustomLinkedList<T extends Number>. It is a generic class to store any types of Number values. It has the following methods:
//	⦁	Upload your solution to GitHub.

public class CustomLinkedList <T extends Number>{

	private CustomLink firstLink;
	private CustomLink lastLink;
	
	CustomLinkedList() {
		
	}
	
	//	⦁	void add(T value, int index): inserts value under the given index. It throws an exception if the index is wrong.
	public void add (T value, int index) {

	}
	
	//	⦁	int size(): returns the length of the List
	public int size() {
		int size = 0;
		
		CustomLink currentLink = firstLink;
		
		while(Objects.isNotNull(currentLink)) {
			size++;
			currentLink = firstLink.getNextLink();
		}
		
		return size;
	}
	
}
